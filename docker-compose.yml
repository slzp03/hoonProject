services:
  db:
    image: postgres:15
    container_name: my-postgres
    environment:
      POSTGRES_DB: hoon
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: